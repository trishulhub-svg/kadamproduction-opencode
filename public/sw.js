// public/sw.js — intentionally empty (service worker removed)
// The stale-while-revalidate caching was causing "bad visuals fixed by hard refresh"
// by serving old CSS/JS chunks after deployment. Admin dashboard doesn't need offline support.
